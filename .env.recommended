# Pharmacy Agent - Recommended Configuration
# Copy this to .env and fill in your actual API key

# ============================================
# OpenAI API Configuration
# ============================================
OPENAI_API_KEY=sk-your-actual-api-key-here

# ============================================
# LLM Model (for parsing & eligibility checks)
# ============================================
# RECOMMENDED: gpt-4o-mini (fast, cheap, high quality)
# - Speed: ~5-8s per batch
# - Cost: $0.15/1M input + $0.60/1M output tokens
# - Quality: Excellent for structured medical data
LLM_MODEL=gpt-4o-mini

# Alternative options:
# LLM_MODEL=gpt-4o              # Slightly smarter, 2x more expensive
# LLM_MODEL=gpt-3.5-turbo       # Faster but lower quality (not recommended)

# ============================================
# Embedding Configuration
# ============================================
# RECOMMENDED: OpenAI embeddings (supports batching)
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small

# If using OpenAI embeddings, dimension is 1536
EMBEDDING_DIMENSION=1536

# Alternative: Ollama (local, free, but slower - no batching support)
# EMBEDDING_PROVIDER=ollama
# EMBEDDING_MODEL=qwen3-embedding:8b
# EMBEDDING_DIMENSION=4096
# OLLAMA_HOST=http://localhost:11434

# ============================================
# Performance Tuning
# ============================================
# Reduce for faster LLM processing (less context to process)
TOP_K_CHUNKS=3

# Document chunking settings
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# ============================================
# Language & Output
# ============================================
OUTPUT_LANGUAGE=turkish

# ============================================
# Expected Performance with Recommended Settings
# ============================================
# Total time for 5 drugs: ~25-35s (vs 150s baseline)
# - Parsing: ~5-10s
# - RAG Retrieval: ~0.5-1s (batched embeddings)
# - Eligibility Check: ~15-25s (batched LLM)
# 
# Cost per report: ~$0.003-0.005 (very cheap!)
